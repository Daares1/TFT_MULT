
section_layout system:sw:main
{
group (run_addr = mem:RAM_XRAM, ordered, contiguous) select ".bss.graphics_instance0_canvasbuffer1_section";
}


section_layout system:sw:main
{
group (run_addr = mem:RAM_XRAM, ordered, contiguous) select ".bss.graphics_instance0_canvasbuffer2_section";
}

